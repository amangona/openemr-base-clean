# Copy to .env.prod on the instance and fill in. NEVER commit the real .env.prod.
# DOMAIN is set automatically by provision.sh to <public-ip>.nip.io.

DOMAIN=REPLACE_WITH_IP.nip.io
MYSQL_ROOT_PASSWORD=CHANGE_ME_root
MYSQL_PASS=CHANGE_ME_openemr
OE_PASS=CHANGE_ME_admin
